version: 2

models:
  - name: bronze_goods_delivery
    description: "goods_delivery_data renamed columns"
   
    config:
      materialized: table              # Can also be 'table', 'incremental', etc.
